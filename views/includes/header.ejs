<header>
  <div>
    <a href="/">Shop</a>
    <nav>
      <ul>
        <% if (locals.isAuth) { %>
         <li>
           <button>Logout</button>
         </li>
        <% } %>
      </ul>
    </nav>
  </div>
</header>